import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
} from 'react-native';

const UserLibrarianScreen = () => {
    return (
        <View style={{ backgroundColor: '#ffffff', flex: 1 }}>
            <Text style={{ color: '#000000' }}>Library</Text>
        </View>
    )
}

export default UserLibrarianScreen;
