import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
} from 'react-native';

const LoginScreen = () => {
    return (
        <View style ={{backgroundColor: '#ffffff', flex: 1}}>
            <Text>Login</Text>
        </View>
    )
}

export default LoginScreen;
